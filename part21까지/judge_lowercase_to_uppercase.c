#include <stdio.h>

int main()
{
    char c1 = 'a';

    printf("%c\n", c1-32);

    return 0;
}

//상수랑 변하지 않는 값, 처음 선언할 때만 값을 할당, 그다음부터는 바꿀수X
/*const int con1(상수) = 10(리터럴);
리터럴은 문자 그대로라는 뜻, 상수는 변수처럼 리터럴이 저장된 공간

*/